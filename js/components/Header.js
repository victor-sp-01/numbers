import Element from "../lib/Element.js"

const Header =()=>{
    const $Element = new Element({
        header:{
            attributes : { class : 'header__71622' },
            contents:{ innerHTML : (`
            
            
            `)}
        }
    })

    $Element.create({ classID : '#root' })
}

export default Header